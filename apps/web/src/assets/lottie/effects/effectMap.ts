/**
 * Profile Effect Lottie asset map — ARCHIVED.
 *
 * Profile effects have been removed (Phase 7 cosmetics consolidation).
 * This file is kept as a stub for import compatibility.
 *
 */

/** Empty map — profile effects archived */
export const PROFILE_EFFECT_ASSET_MAP: Record<string, unknown> = {};

/**
 * Always returns undefined — profile effects archived.
 */
export function getProfileEffectSource(_effectId: string | null): undefined {
  return undefined;
}
