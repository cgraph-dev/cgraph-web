/**
 * Layout Components barrel.
 *
 * Sidebar, mobile nav, and command palette live in `@/layouts/app-layout`
 * and the global-search hook. Only the PageContainer primitive remains here.
 */

export { PageContainer } from './page-container';
export type { PageContainerProps } from './page-container';
