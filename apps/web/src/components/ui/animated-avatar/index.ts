/**
 * Animated Avatar Module
 *
 * Simple avatar component with status indicator, level badge, and premium badges.
 * CSS border system removed — all borders use the Lottie system via ThemedAvatar.
 *
 */

// Main component
export { default as AnimatedAvatar, default } from './animated-avatar';

// Types
export type { AnimatedAvatarProps } from './types';

// Constants
export { SIZE_CONFIG, STATUS_COLORS } from './constants';
